import React from "react";
import "./SearchBar.css";

const SearchBar = () => {
  return (
    <div>
      <input className="input" type="text" placeholder="Seach..." />
    </div>
  );
};

export default SearchBar;
